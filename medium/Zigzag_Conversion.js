var convert = function(s, numRows) {
    let array = new Array();
    for(let i=0 ;i<numRows ; i+=1)
        {
            array[i] = new Array();
        }
    for(let char of s)
        {
            
        }
};